
//THIS script runs with the extension's html page context
//We are sending a message (json object) to the current tab
/*
const tabsPromise = chrome.tabs.query({active: true, lastFocusedWindow: true});
tabsPromise.then(function(tabs) {
    chrome.tabs.sendMessage(tabs[0].id, {action: 'colorText'});
});
*/  


  

  